
import { defineComponent } from 'vue'

const Header = defineComponent({
  name: 'Header',
  render () {
    return (
      <header class="header">
        
      </header>
    )
  }
})

export default Header